<h1>Booking Details</h1>

<p><strong>Flight:</strong> <%= @flight.departure_airport.code %> to <%= @flight.arrival_airport.code %> on <%= @flight.start_datetime.strftime("%B %d, %Y") %> at <%= @flight.start_datetime.strftime("%I:%M %p") %></p>

<h2>Passenger Information</h2>
<ul>
  <% @booking.passengers.each do |passenger| %>
    <li>
      <strong>Name:</strong> <%= passenger.name %><br>
      <strong>Email:</strong> <%= passenger.email %>
    </li>
  <% end %>
</ul>

<%= link_to "Back", bookings_path %>
